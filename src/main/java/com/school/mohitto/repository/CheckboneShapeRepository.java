package com.school.mohitto.repository;

import com.school.mohitto.domain.CheckboneShape;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CheckboneShapeRepository extends JpaRepository<CheckboneShape,Long> {
}
